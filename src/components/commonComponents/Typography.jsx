import React from 'react'

function Typography({variant, color, ...props}) {
    const textClassname = ""
    if(variant === "display-large") text
  return (
    <span></span>
  )
}

export default Typography